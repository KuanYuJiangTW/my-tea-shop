# 派工 prompt 模板（TMPL）

> 用法：選對應模板 → 把【】填掉 → 照表選 agent 與 model（依據 dispatch.md DISP-3）→ 發 Agent 呼叫。
> 三件套（目標與動機／驗收條件／回報格式）已內建，**不要刪掉任何一段**；覺得某段多餘通常代表你還沒想清楚任務。
> 填空原則:【動機】要寫「為什麼做」，subagent 靠它在邊界情況做對取捨；【驗收條件】必須可驗證（能打勾的句子）。

## TMPL-1 搜尋／定位（→ `Explore`，model：純定位 `haiku`、要理解語意 `sonnet`）

```
目標：找出【要找什麼，例：所有計算運費的程式碼】。
動機：【為什麼找，例：要加國際運費規則，需要知道現有邏輯散在哪、有沒有重複實作】。
範圍：【目錄或線索，例：src/ 全部；已知 src/lib/ 有 shipping 相關字樣】。
搜尋廣度：【medium／very thorough】。
驗收條件：
- 每個結果附 file:line 與一句話說明該處做什麼
- 明確說「找完了」或「這些目錄沒掃：原因」——寧可標註未掃，不可假裝掃完
回報格式：條列，每條「file:line — 說明」；總結 ≤5 行說整體結構；禁止貼大段程式碼。
找不到就回報「查無」＋你試過的搜尋詞。
```

## TMPL-2 實作（→ `general-purpose`，model `sonnet`；事前判定很難則 `opus`，見 JUDG-1）

```
目標：【做什麼，例：在 POST /api/orders 加入超商取貨的運費分支】。
動機：【為什麼，例：cvs 通路上線，運費 NT$60、滿千免運不變——分支錯了會少收錢】。
規格與參考：【openspec 路徑或既有範例，例：規格見 openspec/specs/cvs-pickup/；照 src/app/api/orders/route.ts 現有分支寫法】。
範圍限制：只改【檔案清單或目錄】；不要動【明確排除，例：資料表 schema、其他 API】。
驗收條件：
- [ ] npm run test 全綠（含既有測試）
- [ ] 【功能驗收，例：cvs+訂單金額 999 → 運費 60；cvs+1000 → 運費 0】
- [ ] 使用者可見字串進 messages/zh-TW.json 與 en.json，不硬編碼
- [ ] npx tsc --noEmit 無新增錯誤（npm run lint 已失效，見 JUDG-5）
回報格式：改了哪些檔（file:line 級）、每條驗收條件的證據（測試輸出貼末段 ≤10 行）、
未盡事項或不確定處明列。禁止貼整檔。長 diff 不用貼——我會看 git diff。
遇到規格模糊：停下來回報兩種解讀＋你的建議，不要自行猜測後硬做。
```

## TMPL-3 重構（→ `general-purpose`，model `sonnet`）

```
目標：【重構什麼，例：把三個 API route 重複的 rate-limit 邏輯抽成共用函式】。
動機：【例：三份副本已經出現行為不一致，之後要統一調參數】。
不變式（最重要）：對外行為完全不變——【列出可觀察行為，例：各 route 的限流閾值、回應碼、錯誤訊息】。
範圍限制：只動【檔案清單】；不順手修別的（看到其他問題記下來回報，不要動手）。
驗收條件：
- [ ] 重構前先跑 npm run test 記錄基準，重構後全綠且無新增 skip
- [ ] 【行為不變的抽查方式，例：三個 route 各發一次超限請求，回應與重構前相同】
- [ ] 淨行數變化（新增-刪除）為【負值／小於 N】——重構越改越肥要說明理由
回報格式：抽出的新結構一句話說明、file:line 對照（舊 → 新）、測試前後輸出末段、
順手發現但沒動的問題清單。
```

## TMPL-4 研究／查證（→ `general-purpose`；Claude Code 相關文件用 `claude-code-guide`。model `sonnet`）

```
問題：【要查什麼，例：Next.js 16 App Router 的 route handler 可否讀 raw body 做 HMAC 驗簽】。
動機：【例：ECPay 回調驗簽需要 raw body，決定要不要換寫法】。
查證要求：
- 只接受有出處的答案：官方文件優先，附 URL；查不到就寫「docs don't specify」
- 區分「文件明說」與「你的推論」，推論要標註
- 版本要對：本 repo 用【版本，例：next ^16.2.2，見 package.json】
驗收條件：每個結論都有 citation URL 或標明「未查到」；直接回答我的決策問題，不要只丟資料。
回報格式：先一句話結論，再逐點依據（≤15 行）；長篇研究寫到【檔案路徑】只回摘要與路徑。
```

## TMPL-5 驗收／審查（→ `checker`，model 已內建 sonnet+effort high；diff 品質另跑 `code-review` skill）

```
請驗收以下交付物。你只驗收、不修東西；不要相信本訊息以外的任何「已完成」宣稱。
產物路徑：【檔案清單或 diff 範圍】。
測試指令：【例：npm run test；如需實跑，附啟動方式】。
驗收條件（逐條裁決 PASS/FAIL/CANNOT VERIFY，附證據）：
- [ ] 【條件1，可驗證句】
- [ ] 【條件2】
- [ ] 【批次修改才加：抽驗至少 2 處＋隨機 1 處，列出抽了哪幾處】
回報格式：照你的固定格式（總裁決先行）。
```

## 完整範例（TMPL-2 填好的樣子，對照用）

```
目標：在 POST /api/reviews 加「同一使用者對同一商品 24 小時內只能留一則評價」限制。
動機：刷評價灌水已出現，會汙染商品排序；限制錯了會誤傷正常補評，所以邊界要準。
規格與參考：openspec/specs/coupon-rate-limit/ 有既有限流模式可參考；DB 查詢照 src/lib/ 現有 supabase client 用法。
範圍限制：只改 src/app/api/reviews/ 下的 route 與對應測試；不動資料表 schema。
驗收條件：
- [ ] npm run test 全綠
- [ ] 24h 內第二則 → 429 與明確錯誤訊息；24h 零 1 秒後 → 可發
- [ ] 錯誤訊息進 messages/ 兩語系
- [ ] npx tsc --noEmit 無新增錯誤（npm run lint 已失效，見 JUDG-5）
回報格式：改動檔案 file:line、四條驗收各附證據（測試輸出末段）、不確定處明列。
遇到規格模糊：停下回報解讀選項，不要猜。
```

## 通用規則（所有模板適用）
- prompt 裡的路徑、指令、版本號，發出前自己先驗證存在（`ls`、`git log`、package.json），錯的輸入必然得到錯的輸出。
- 一次派工一個目標。兩個目標拆兩次派工，或明確標主次。
- subagent 回報若違反回報合約（貼整檔、超長、無證據），下次派工在 prompt 開頭加一行：「上次回報過長，這次嚴格遵守回報格式，超過即重做」。
